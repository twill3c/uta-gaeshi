# TEST_SPEC.md — uta-gaeshi

<!-- scaffold template v1.22.0 から展開(2026-08-31) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

本プロジェクトの期待値は**すべて外部データの実測**に由来する。件数を定数で書くと
上流の改訂と我々の回帰が区別できなくなるため、次の二層構造を取る。

| 層 | 実装 | 落ちたときの意味 |
|---|---|---|
| 上流の刻印 | `pipeline/pins.py` の `PINNED_SHA256`(実測日 2026-08-31) | **上流が改訂された**。実測し直して刻印と期待値を更新する(`chore: repin upstream`) |
| 構造の不変量 | `pipeline/gates.py` の `check_structure` | **我々の回帰**。コードを直す |

`gates.diagnose()` が両者を突き合わせ、どちらであるかを言い分ける。
件数(`BOOKS` / `GREEK_LINES` / `UNITS`)は刻印とセットでのみ意味を持つ定数である。

不変量の本体は件数ではなく**集合の性質**で書く:
分割の排他性・被覆の網羅性・行番号の一意性と単調性・既知欠番の集合一致。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `data/units.json` | PerseusDL/canonical-greekLit(実測 2026-08-31) | 外部権威。sha256 で刻印 |
| 既知欠番 `{10:456, 16:101, 23:49}` | 実測。校訂で削除され番号だけ残る行 | 外部データの性質。埋めてはならない |
| 既知の組版順逆転 `{3:304, 14:63}` | 実測。Loeb 版の校訂判断 | 欠陥ではない。整列した事実として記録 |
| 出典欠陥 2 件(巻6 錨重複 / 巻16 錨範囲外) | 実測。loop_001 の DATA-QUAL | 修復し `data/repairs.json` に残す |
| 反復行・定型句(L2) | 原典の n-gram 全数集計。決定論的 | **循環なし**。和訳の検査に使える |
| 話者(L2) | 発話導入定型 + 格変化。希英の二経路で突合 | 一致率を測る対象であって前提ではない |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-02 | 本物のコーパスに G-01 を適用 | 違反 0 |
| T-002 | F-03 | 修復記録が既知の欠陥と一致 | 違反 0 |
| T-003 | F-02 | 単位が原典 12,107 行を重複なく分割 | 重複 0・被覆 12,107 |
| T-004 | F-03 | 既知欠番が埋められていない | 欠番のまま |
| T-005 | F-02 | 全単位が希・Murray・Butler を持つ | 空 0 |
| T-006 | F-02 | 単位の区間が内包する行と整合 | min/max が区間内 |
| T-101 | F-01 | 上流の sha256 が刻印と一致 | 一致 |
| T-201〜T-210 | G-06 | 陽性対照(単位除去・重複割当・訳文空・原典行空・巻欠落・欠番の捏造・単調性破壊・区間逆転・修復記録の消失・逆転記録の消失) | **すべてゲートが落ちること** |

陽性対照は「ゲートが常に合格を返す飾りではない」ことの確認であり、
G-01 の各不変量に一対一で対応させる。不変量を追加したら対照も追加する。
